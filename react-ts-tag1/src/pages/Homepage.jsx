import Button from "../components/globals/Button"

const Homepage = () => {

    return <div>
        <h1>Homepage</h1>
        <Button
            text="submit" handler="function()"
        />
    </div>
}

export default Homepage